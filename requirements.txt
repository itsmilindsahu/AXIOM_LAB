# BIS Standard Discovery — Requirements
# Install: pip install -r requirements.txt

# Core ML / Embeddings
sentence-transformers>=2.7.0
torch>=2.0.0

# Vector store
faiss-cpu>=1.7.4

# Sparse retrieval
rank-bm25>=0.2.2

# Graph
networkx>=3.2

# App
streamlit>=1.35.0
pandas>=2.0.0

# LLM rationale (optional — for Claude-powered rationale generation)
anthropic>=0.30.0

# Offline / CI fallback (TF-IDF encoder when HuggingFace is unavailable)
scikit-learn>=1.3.0

# Visualisation (knowledge graph diagram, evaluation plots)
matplotlib>=3.7.0

# Utils
numpy>=1.24.0
tqdm>=4.66.0
